<UserControl x:Class="NoteTrainerThing.Views.SoundPlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="50"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="150" />
        </Grid.ColumnDefinitions>
        <Button x:Name="TogglePlay" Grid.Column="0" Width="125" Height="25" Content="{Binding PlayText}" />
        <Button x:Name="PlayNext" Grid.Column="1" Width="125" Height="25" Content="Next" />
    </Grid>
</UserControl>
